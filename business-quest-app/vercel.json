{
  "ignoreCommand": "git fetch -q --deepen=1 origin $(git rev-parse HEAD) 2>/dev/null;p=$(git rev-parse HEAD^@);[ -z \"$p\" ]&&exit 1;for x in $p;do git diff --quiet $x HEAD -- . ../family-quest-app/src ../package-lock.json||exit 1;done;exit 0"
}
