{
  "ignoreCommand": "bash -c 'cd \"$(git rev-parse --show-toplevel)\" && for p in $(git rev-parse HEAD^@); do git diff --quiet \"$p\" HEAD -- business-quest-app family-quest-app/src package-lock.json || exit 1; done; exit 0'"
}
