{
  "ignoreCommand": "bash -c 'cd \"$(git rev-parse --show-toplevel)\" && if [ \"$(git rev-parse --is-shallow-repository)\" = \"true\" ]; then git fetch --quiet --deepen=1 origin \"$(git rev-parse HEAD)\" 2>/dev/null || true; fi; parents=\"$(git rev-parse HEAD^@ 2>/dev/null)\"; if [ -z \"$parents\" ]; then exit 1; fi; for p in $parents; do git diff --quiet \"$p\" HEAD -- business-quest-app family-quest-app/src package-lock.json || exit 1; done; exit 0'"
}
