import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.howlingcreative.familyquest',
  appName: 'Family Quest',
  webDir: 'dist'
};

export default config;
