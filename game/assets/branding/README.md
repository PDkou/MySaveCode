# 브랜딩 에셋

이 폴더에 `logo.png` 파일을 넣으면 인트로 화면(`scripts/Intro.gd`)이 자동으로 로고 이미지를 사용합니다.
파일이 없으면 "HOWLING / CREATIVE STUDIO" 텍스트 워드마크로 자동 대체됩니다 (코드에 안전장치 있음).

- 경로: `game/assets/branding/logo.png`
- 권장: 정사각형에 가까운 비율, 512px 이상, 검정 배경 포함(현재 로고와 동일하게)
