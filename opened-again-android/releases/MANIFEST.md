# Release manifest

Nothing here has ever been uploaded to Play Console — this table is a version↔commit log for a
sandbox that can't create git tags, not a store release history yet.

| Version | Version code | Commit | Status | Notes |
|---|---:|---|---|---|
| 0.2.0 | 2 | pending | development | WebView shell + native usage-analysis bridge + incident cards |
| 0.5.0 | 5 | pending | development | Final v1.2 asset pack wired into `index.html` (badges, card templates/examples, character poses, backgrounds, logo, app icon); imported into the `MySaveCode` monorepo as `opened-again-android/` |
| 0.6.0 | 6 | pending | development | `ShareCardRenderer` now draws the same final asset pack (background, card template art + tint, rarity badge, character pose, logo) instead of flat shapes/text; `shareIncident()` takes the in-app selected language for the logo |
| 0.7.0 | 7 | pending | development | Fixed a wrong-asset-folder bug from 0.6: card backgrounds now use `cards/frames/*` (the real blank frames) instead of `cards/templates/*`/`cards/examples/*` (finished mockups with baked-in placeholder text), which were also getting destructively cropped due to an aspect-ratio mismatch. Verified by rendering index.html in headless Chromium and inspecting screenshots. Also fixed a self-inflicted nested-comment compile error introduced by that same commit. |
| 0.8.0 | 8 | pending | development | Fixed `preview-hidden.html`'s broken `moni_avatar.png` reference (real character assets now); refreshed `preview-board.html`'s stale v0.3 label and added a missing HIDDEN swatch, catching and fixing a layout overflow it introduced along the way. |
| 0.9.0 | 9 | pending | development | Fixed low-contrast white text on the Records tab's "investigation report" banner (background is actually a light cream tint over a night photo, not dark) -- switched to the standard dark ink/muted text colors. |
| 0.10.0 | 10 | pending | development | Added a launcher icon -- AndroidManifest.xml never had `android:icon`/`android:roundIcon` and no `res/mipmap-*` existed, so the app installed with the system default icon since v0.1. Generated `ic_launcher`/`ic_launcher_round` at all 5 standard densities from `visual/app-icon/app_icon_phone.png`. Legacy (non-adaptive) icon only for now -- see docs/DEVELOPMENT_HISTORY.md v0.10 for why. |
| 0.11.0 | 11 | pending | development | Edge-to-edge fix: on API 30+ (which Android 15/targetSdk 35 forces regardless of app opt-in) MainActivity now explicitly enables edge-to-edge with transparent system bars instead of relying on deprecated solid statusBarColor/navigationBarColor; index.html's modal bottom padding now also respects `env(safe-area-inset-bottom)` (its `.app` root padding already did). Needs on-device confirmation, especially on gesture-nav devices. |
| 0.12.0 | 12 | pending | development | Fixed asset-layering bugs found via real on-device screenshots: ShareCardRenderer's "backdrop" (share_template_*.png) was actually a finished card with its own frame+character baked in, doubling up against the separately-drawn rarity frame -- swapped to the plain bg_pattern_beige.png tile and dropped the now-redundant badge draw. index.html's `.card` background reverted from cards/frames/*.png (which only covered a band at the top of the content-driven-height card) back to the original aspect-independent gradient-tint system. Archive now shows only the single highest discovered rarity instead of joining every tier ever found. |
| 0.13.0 | 13 | pending | development | Stopped using `cards/examples/*.png` (a folder explicitly documented as reference-only) for archive-grid thumbnails, after the director pointed out the folder's own name says so -- replaced with a live composite of the character pose + the existing per-rarity `--scene` gradient tint (new `archiveRarityClass()`; `archiveThumb()` removed). Direct file inspection confirmed `cards/examples/` and `badges/*.png` both have real source-crop bleed from adjacent sheet items (`cards/frames/`, `character/`, `backgrounds/`, `logo/` are clean); `badges/` is still live in `rarityBadge()` and is a genuine asset defect that needs re-cropping from the design pipeline, not a code fix -- flagged in docs/OPEN_ISSUES_AND_NEXT.md. |
| 0.14.0 | 14 | pending | development | Design pipeline replied to all 3 asset requests with a full "v0.13 current clean" pack: 5 of 6 badges re-cropped, a proper adaptive launcher icon (transparent character foreground + solid background, wired via `mipmap-anydpi-v26`), and official confirmation that `share_template_*` is static-promo-only (current dynamic share-card composition was already correct). The pack also carried a much larger reorganization (nearly every asset re-exported, `character/additional/` merged into `character/basic/`, `effects/`+`speech/` merged into `props/`, `character/expressions/` fully renamed, `bg_home_*` renamed to `bg_room_*`) plus 3 more contaminated character files caught and removed (`moni_sit_phone.png`, `moni_sleep.png`, `moni_thinking.png`, replaced with new expression poses). All runtime asset references in `index.html`, `ShareCardRenderer.kt`, and `preview-hidden.html` updated to match. |
| 0.15.0 | 15 | pending | development | Fixed a real-device bug: exported share-card text (title/detail) overflowed past the card frame's visible border, overlapping the frame art's own hat/paw-medallion corner ornaments. Pixel-measured `cards/frames/frame_*.png` directly to find how far those ornaments actually intrude (~15-17% of card height, consistent across all 6 rarity frames) and raised `ShareCardRenderer`'s `contentTop` inset from 0.12 to 0.22 of the card height. Same investigation found and fixed a second collision: the bottom-right "MONI CASE FILE" caption sat under the frame's magnifier ornament -- moved it to anchor at `leftColRight` instead of the card's right edge. Verified by reproducing the exact layout math in Python/Pillow against the real frame asset (couldn't run the real Kotlin Canvas code in this sandbox) -- confirmed the bug reproduces pre-fix and is clean post-fix. |
| 0.16.0 | 16 | pending | development | Fixed two more real-device bugs found while re-testing v0.15: `bg_pattern_beige.png` is a small 445x535 seamless *tile*, but `render()` was `drawCover()`-stretching it ~2.4x to fill the whole 1080px canvas -- blowing up every paw/hat icon and making the card look small by comparison. Switched to a `BitmapShader` with `TileMode.REPEAT` (a proper background-repeat). Also fixed longer punchline text wrapping wide enough (up to ~95% of card width) to run past the frame's actual visible border (~87%, pixel-measured) -- narrowed the wrap width with a 16%-of-width right inset. Verified via a Playwright/Chromium re-render of the same layout math against the real assets (this time with real Korean font rendering) reproducing both bugs pre-fix and clean post-fix. |
| 0.17.0 | 17 | pending | development | Found `bg_pattern_beige.png` isn't actually a seamless tile (its own edges don't match), which is why v0.16's tiling showed a visible seam grid on a real device -- flagged to design (`docs/ASSET_REQUESTS_FOR_DESIGN.md` #4) and switched to a flat fill using the pattern's own base tone as a stopgap (no seams possible, and it reads as one unified background). Also found the real cause of the punchline/border collision reported since v0.15: MONI's character image (bottom-anchored, width-constrained for every pose) was tall enough to still have pixels at the punchline's row, so the two visibly overlapped. Raised `footerTop` (MONI's box bottom) from a fixed pixel offset to 28% of the card's own height, freeing a guaranteed-clear strip below for the punchline; logo/caption now anchor dynamically off the punchline's actual wrapped height (1-2 lines, measured against all 14 `punch{}` strings) instead of a fixed offset from the card bottom. |
| 0.18.0 | 18 | pending | development | Director wanted the decorative background pattern kept, not replaced with v0.17's flat color. Switched `BitmapShader`'s tile mode from `REPEAT` to `MIRROR` -- alternating a flipped copy at every repeat guarantees matching edge colors regardless of whether the source tile is seamless, verified pixel-by-pixel against a 2x2 mirrored composite of the real asset. Also found the punchline collision's real cause: it's not the plain border (measured at ~87% of card width) but the frame's much bigger magnifier ornament, which pixel-measured as intruding to ~64% of card width in the row band the punchline occupies. Raised `footerTop` to 34% of card height, shrank the punchline to 38px/46px line-height at 55% max width, and re-verified all 14 `punch{}` strings fit within a 3-line worst case inside the new footer budget. |
| 0.19.0 | 19 | pending | development | Fixed a code bug present since v0.15, not an asset problem: `pad` (the shared left x-offset for title/detail/punchline/logo) had been a fixed 56px (8.1% of card width) the whole time, but pixel-measuring the frame's actual left border (on both frame_normal.png and frame_legendary.png) found the real interior starts at ~13.5% -- every text element had been starting on/over the border stroke instead of inside it. Changed `pad` to 14% of card width and re-derived the punchline's max width from the corrected pad so the earlier magnifier-avoidance margin didn't get eaten by the wider left inset. Verified by drawing the measured 13.5% boundary as a guide line over the real frame art in a Chromium re-render: text starts on/left of that line before the fix, clearly right of it after. |
| 0.20.0 | 20 | pending | development | Fixed the card itself looking small in the SQUARE export, separate from v0.16's background-scale issue. `outerTop`/`outerBottom` (110f) were the real bottleneck: fitting the frame's ~0.8 aspect ratio into a 1:1 1080x1080 canvas is height-limited, so that vertical margin capped the card at only 688x860 (64%x80% of the canvas), leaving large empty side margins. Cut `margin`/`outerTop`/`outerBottom` to 50f for SQUARE specifically -- STORY's large margins (330f) are intentional and untouched, reserving the top/bottom zones Instagram Stories overlays its own UI into. Every interior layout constant is already expressed as a fraction of `rect.width()`/`rect.height()`, so the whole card's content scaled up automatically with no separate fixes needed. |
| 0.21.0 | 21 | pending | development | Fixed a genuine source-asset crop defect (not a code bug): `logo_ko.png`/`logo_jp.png`'s paw-icon decoration was saved cut off at the image canvas edge (`logo_ko.png` on the right, `logo_jp.png` on both sides), visible in the home-screen header on every app open. Design pipeline re-exported both from the existing files per director's request (crop fix only, no redraw) -- new files verified fully transparent on all four edges (crop gone) and visually near-identical text styling via a same-background side-by-side comparison. Same asset paths reused, so both `index.html`'s header and `ShareCardRenderer.kt`'s share-card logo pick up the fix with no code change. Verified against the real `index.html` via Playwright in both KO/JP modes -- zero broken images, paw icon renders complete. |
| 0.22.0 | 22 | pending | development | Resolved the standing share-card background request (`ASSET_REQUESTS_FOR_DESIGN.md` #4): design delivered a full pack (`openedagainsharebackgroundsandlogosv0.19.zip`) with a single non-tiled 1080x1080/1080x1920 background per rarity (NORMAL/RARE/EPIC/LEGENDARY/HIDDEN_01/HIDDEN_02 + a common fallback), replacing v0.18-v0.21's `TileMode.MIRROR` tiling workaround entirely. `ShareCardRenderer.kt` now picks the right file by rarity/opal-family/format via a new `backgroundAsset()`/`commonBackgroundAsset()` lookup and draws it full-bleed with `drawCover()`. The same pack also carried a re-fit of the v0.21 logo files back to the original legacy canvas size (471x150/575x130, same design, safe transparent margins) -- swapped in as the director's designated final version. |
| 0.23.0 | 23 | pending | development | Integrated the final visual asset handoff answering `docs/UI_VISUAL_DIRECTION_REQUEST.md`: 14 dedicated per-IncidentType illustrations (`incidents/card_ready/*.png`) replace the 6-7 shared generic character poses everywhere (`index.html`'s `incidentVisual()`/`card()`/`archive()` and `ShareCardRenderer.kt`'s new `incidentIllustrationAsset()`), branching on each file's `overlay`/`scene` render_mode; rarity badges switched from a baked round PNG to a CSS pill built from the long-unused `.rarity` class plus new small `rarity_symbols/*.png` icons, per design's own instruction not to bake the pill into an image; empty-state and locked-card art added to the cases/archive views. Found and fixed a real bug while wiring the illustrations into `ShareCardRenderer.kt`: its character box is portrait-shaped but the new art is landscape, so a naive `drawContain()` made overlay characters tiny and left scene illustrations mostly empty space -- fixed with an alpha-bounding-box trim for overlay art and a `drawCover()` crop for scene art. Onboarding screens and labeled UI icons from the same pack are copied into assets but not yet wired in (deferred: this app has no onboarding flow to hook them into yet, and the tab bar is text-based, not icon-based -- both are real feature work, not asset swaps). Launcher icon left untouched per the pack's own "not final" notice. |
| 0.24.0 | 24 | pending | development | Wired up the two-screen onboarding deferred from v0.23. The source art (941x1920, ko/jp, full-screen comps with buttons baked into the pixels) is used as-is per design's own instruction; real taps are transparent buttons positioned as % of the image's own box (locked to its aspect ratio via CSS so they track the art on any device screen ratio), their coordinates measured directly against the real PNGs. Gated by a new `state.settings.onboardingSeen` flag (persisted through the existing settings-save path, no new persistence code needed) -- `render()` shows only the onboarding screen until both are completed, then proceeds to the app's normal permission/tab flow. The "allow notifications" button requests Android's real runtime POST_NOTIFICATIONS permission (new `NativeBridge.requestNotificationPermission()` + manifest entry) rather than silently doing nothing, since this app had zero notification infrastructure before this and the art shouldn't promise something that's faked -- fire-and-forget, since there's no notification feature yet to act on the grant/deny result either way. |
| 0.25.0 | 25 | pending | development | Director felt the case cards' left (text) side looked flat next to the now-illustrated right side. Added a low-opacity `.card-pattern` layer reusing the existing per-rarity share-card backgrounds (`backgrounds/share/1080x1080/bg_*_square.png`, paw+hat motif) as subtle texture behind the card's text -- no new assets, and it incidentally makes the web card and the native share card use the same background pattern language. Verified legibility holds at every rarity, including HIDDEN's dark navy card. |
| 0.26.0 | 26 | pending | development | Fixed the actual CI failure on v0.24: `AndroidManifest.xml`'s new POST_NOTIFICATIONS comment contained a double-hyphen (`--`), which XML comments forbid anywhere except the closing `-->` -- the manifest merger couldn't parse the file at all, breaking `assembleDebug` for both v0.24 and v0.25 (v0.25 didn't touch this file, so it inherited the same break). Every other check this session had been local simulation/Playwright only; this is the first real GitHub Actions build failure, caught by actually reading the failed job's logs rather than assuming green. Fixed the wording, verified with `xml.etree.ElementTree.parse()`. |
| 0.27.0 | 27 | pending | development | Director liked v0.25's card texture but wanted EPIC/LEGENDARY to feel more impactful than NORMAL/RARE (left as-is per explicit approval; HIDDEN already has its own dark theme). Added, with zero new assets: a large, very faint rarity-symbol watermark (star/crown, reusing `brand/rarity_symbols/*`) in the card's bottom-right corner; a diagonal shine-sweep animation across the whole card every 4.5s (a common high-rarity gacha/card-game cue); and a color-matched `box-shadow` bloom (purple for EPIC, gold for LEGENDARY) layered on top of the existing shadow so the card reads as lifted off the page. Verified text stays fully legible through the shine pass and watermark at both rarities, and that NORMAL/RARE/HIDDEN are visually unchanged. |
| 0.28.0 | 28 | pending | development | Director: v0.27's static treatment still didn't feel special enough. Turned every static effect into a live animation instead: the glow is now a breathing pulse (ring width and brightness cycle every 2.6s via new `epicPulse`/`legendaryPulse` keyframes, replacing the fixed `box-shadow`), the star/crown watermark spins continuously (16s per rotation, opacity bumped .16->.22, size 120->130px), and the shine sweep is now tinted with the rarity's own color (purple/gold, was plain white) and sweeps faster (3.4s, was 4.5s). Verified the pulse is actually animating by screenshotting the same card 1.3s apart and confirming the glow ring's width/brightness visibly differs between frames. |
| 0.29.0 | 29 | pending | development | Real on-device feedback surfaced 4 issues with v0.28: (1) the card-detail modal's title/detail/punchline text became unreadable under the shine sweep -- fixed with `clip-path` confining `.card-shine` to the card's right side, never touching the text column; (2) the animations caused real lag -- root-caused to `box-shadow` value animation and `background-position` animation, neither of which the browser can composite (both force a full repaint every frame); removed the shadow pulse entirely (back to a fixed bloom) and rebuilt the shine as a `transform`-only sliding bar (`.card-shine-bar`), which conveniently also fixes issue 1; (3) archive-grid thumbnails for "overlay"-mode incidents (QUICK_EXIT, REENTRY, etc.) showed just a small floating character on a flat color -- `archive()` was silently dropping `incidentVisual()`'s background photo that `card()` already uses, now fixed so archive matches the main cases view; (4) director felt archive illustrations need remaking -- likely was a symptom of (3); left open pending confirmation after the fix. |
| 0.30.0 | 30 | pending | development | Director: reusing shared room/city backgrounds in the archive grid (v0.29) still wasn't good enough on its own. Found the deeper issue: "overlay"-type illustrations only draw content in a small central portion of their 1200x675 canvas (same wasted-transparency issue documented in ShareCardRenderer.kt's opaqueBounds()), so `object-fit:contain` rendered the character much smaller than it needed to be. Switched those thumbnails to `object-fit:cover` (a new `.overlay-fill` class) so the image scales up and center-crops instead, filling the tile far more -- locked-card art keeps `contain` since the whole graphic matters there. Verified all 11 affected archive tiles visibly fill their frame much better than before; left open whether this is sufficient or the 9 overlay types still need dedicated full-scene art from design. |
| 0.31.0 | 31 | pending | development | Infrastructure-only, no visible change: director's illustration-redraw request (docs/ASSET_REQUESTS_FOR_DESIGN.md #6) grew to include per-rarity variants (Rarity is computed dynamically per detection, not fixed per IncidentType, so "different art per rarity tier" needs a distinct file per (type, rarity) pair). Added `RARITY_ILLUSTRATION_VARIANTS`/`incidentArt()` in index.html and the matching `rarityIllustrationVariants`/`incidentIllustrationAsset(type, rarity)` in ShareCardRenderer.kt (renamed the old single-arg lookup to `incidentIllustrationBase()`) -- both variant sets start empty, so every lookup falls back to today's single per-type illustration with zero behavior change, but the moment design delivers a variant file, adding one key string lights it up with no further code changes. Verified via Playwright that cases/archive render identically to v0.30. |
| 0.32.0 | 32 | pending | development | Delivered all 50 illustrations requested in ASSET_REQUESTS_FOR_DESIGN.md #6 (12 incident types x 4 rarities + 2 HIDDEN), GPT-generated by the director from docs/GPT_IMAGE_PROMPTS.md after a mid-delivery prompt revision (a first LEGENDARY-tier result drifted into unrelated castle/banner/conquest imagery -- fixed by adding an explicit "keep the same scene, don't invent new environments" constraint to the tier instructions). Wired all 48 "TYPE_RARITY" keys into index.html's RARITY_ILLUSTRATION_VARIANTS and ShareCardRenderer.kt's rarityIllustrationVariants (infrastructure from v0.31), and placed the files under incidents/card_ready/. Since every new illustration is now a complete painted scene (not a transparent overlay cutout), the 9 previously-"overlay" incident types are now treated as "scene" everywhere (incidentVisual()'s render_mode, ShareCardRenderer.kt's isSceneIllustration()) -- the v0.29/v0.30 overlay-compositing and opaqueBounds()/drawContain() crop paths are now dead code, kept but unreachable. Verified via Playwright screenshots of the cases and archive tabs (no broken images, illustrations fill their full frame) and the LEGENDARY card-detail modal (shine/emblem effects still don't obscure text). Open item: the new assets bring incidents/card_ready/ to ~91MB; WebP/resolution-reduction follow-up pending director sign-off. |
| 0.33.0 | 33 | pending | development | Director: clicking an acquired archive card should show a modal with everything collected for it, broken down by rarity. Added openArchiveDetail(type), wired to a new onclick on non-locked .archive-card tiles. The 12 regular incident types get a 2x2 NORMAL/RARE/EPIC/LEGENDARY grid, each cell showing that rarity's actual illustration once state.history.discoveries[type] contains it, else a "?" placeholder, plus an "N/4 collected" header. The 2 HIDDEN types (always Rarity.HIDDEN, no rarity spread to show) get a single hero view of their one illustration + name + punchline instead. Also fixed a real UX gap surfaced while building this: since only HIDDEN types are ever visually "locked" in the archive, a regular type with zero actual discoveries still reads as "found" and was clickable but would previously do nothing -- the modal now always opens for those (showing 0/4, all four cells locked) rather than a dead click; HIDDEN types still never open before they're actually found. Verified via Playwright across a found-one-rarity type, a found HIDDEN type, a zero-discoveries type, and a real DOM click (not just a direct function call) -- zero broken images in all cases.
| 0.34.0 | 34 | pending | development | Re-encoded all 62 files in incidents/card_ready/ (the 50 active illustrations plus the 12 now-unused pre-v0.31 base files) from PNG to WebP q85 to address the ~91MB size director flagged after v0.32's illustration delivery. Compared PNG re-compression (only reaches ~56-57% of original -- a poor format fit for painted/gradient scenes), JPEG, and WebP on samples; WebP q80-85 got to 5-11% of original size with no visible quality loss, confirmed with the director via an actual converted sample before doing all 62. Updated index.html's incidentVisual() base paths and incidentArt()'s suffix-swap regex (now handles both .png and .webp), and ShareCardRenderer.kt's incidentIllustrationBase() paths and incidentIllustrationAsset()'s suffix logic (now extension-agnostic via lastIndexOf('.')) -- no decoding-side changes needed since both BitmapFactory.decodeStream() and WebView natively handle WebP by content, not extension. Folder size: 94.3MB -> 7.2MB. Verified via Playwright across cases, archive, and the archive-detail modal -- zero broken images, visually identical to the pre-conversion screenshots.
| 0.35.0 | 35 | pending | development | Director asked why the delivered UI icon set (docs/UI_VISUAL_DIRECTION_REQUEST.md item 5) wasn't in use, then pointed out the tab bar was supposed to be at the bottom, not the top -- re-diffing all 3 target-visual-direction mockups against the running app surfaced three more real gaps beyond icons: tab bar position, the home screen's featured-card+compact-list structure, and the incident detail view being a full-screen page rather than a bottom-sheet modal. Fixed all four, prioritized by the director: (1) tabs() rebuilt as a fixed bottom bar (home/records/collection/settings, icon+live-text-label) using the unlabeled icon set, cropped to their actual opaque content first (10 files were 512x512 canvases with a ~110px glyph, same wasted-margin issue hit twice before); (2) a real settings screen (previously nonexistent) with working language/notification-permission/usage-access rows and static theme/data/help/about rows; (3) cases() now shows one featured full card plus the rest as compact icon+title+rarity one-line rows (compactCase()); (4) openDetail() replaced its bottom-sheet modal with a full-screen page (detailPage(), driven by new state.detailItem) carrying its own back/rarity-pill/share header and a stats box built only from whatever metric fields the incident actually has (no fabricated data). Found and fixed two real bugs surfaced while building this: the HIDDEN rarity class's dark-page styling leaking into the detail page's white stats box (illegible near-white-on-white text) once reused outside its original card-only context, and a metric-label lookup table built as a frozen top-level object whose l() calls froze at page-load language instead of updating on language switch. Verified via Playwright across all 4 tabs, real clicks on the featured card/compact rows/back button, the v0.33 archive rarity modal (renamed to closeOverlay() to stop colliding with the repurposed closeDetail()), and a full ko/ja language switch reaching every new string including the stats labels -- zero broken images.
| 0.36.0 | 36 | pending | development | Fixed 7 real-device feedback items in one pass. (1) Onboarding screen cropped on real phones: .onboard-box sized itself by height:100% + a fixed aspect-ratio, which forces it wider than the viewport on any screen narrower/taller than the source image (confirmed via Playwright at 393x851: box rendered 479px wide, 43px cropped off each side) -- switched to CSS min()-based "contain" sizing (width:min(100vw,100vh*941/1672), height mirrored) so the full design always fits with letterboxing instead of cropping, on any device ratio. (2) Added android:enableOnBackInvokedCallback="true" to the manifest for Android 16 predictive-back readiness (targetSdk 36 already, but the app hadn't opted in) -- MainActivity's existing onBackPressed() override keeps working via the platform's compatibility path. (3) The card shine effect read as unnatural for two real reasons found by freeze-framing the animation: it swept right then immediately reversed left every cycle (a visible "wiper" motion, not how light behaves) and its peak brightness fully whited out the share buttons/illustration underneath -- rebuilt as a genuine one-way sweep (holds off-screen between passes) at much lower peak opacity. (4) Buttons across the app had accreted inconsistent radius (13-16px) and padding with several under Android's ~48dp minimum touch target (icon-btn was 42x42) -- introduced shared --btn-radius/--btn-min tokens and applied them to every button class. (5) Header title felt disconnected from the icon, worse in Japanese: logo_ko.png/logo_jp.png had transparent margins baked into the pixels (7.8% and a striking 20% of canvas width respectively), directly explaining why Japanese was worse -- cropped both to their opaque content. (6) Share card image felt like an illustration pasted onto a card frame with no harmony: ShareCardRenderer.kt's scene box still used its pre-v0.32 flat-rectangle compositing, designed for transparent character cutouts, not today's fully-painted-scene illustrations with their own independent backgrounds -- added rounded-corner clipping, a soft BlurMaskFilter shadow, and a rarity-palette border stroke so it reads as a deliberately placed photo rather than a foreign sticker; verified via a Python/PIL simulation of the same coordinate math (no JVM/Android runtime available in this sandbox to run the real Kotlin Canvas code, consistent with this renderer's established verification gap -- still needs on-device confirmation). (7) covered by 1-6. Verified 1/3/4/5 via Playwright measurements (box rects, animation-frame pixel diffs, button bounding boxes, ko/ja header screenshots), 2 via manifest XML parse (avoiding the illegal "--" comment mistake from v0.26), 6 via the PIL simulation; full 4-tab + archive-modal + language-switch regression suite re-confirmed, zero broken images.
| 0.37.0 | 37 | pending | development | Full TCG-style ("Pokemon/Yu-Gi-Oh card") redesign of the share card, replacing the v0.36 text-left/image-right frame layout entirely. Root architecture fix for a recurring "카드마다 사이즈가 다 틀리다" problem: `cards/frame_bg/<rarity>.webp` is now pure atmospheric background art (no baked panel geometry), while every piece of content (art window, header/info panels, rarity chip, foil title, emblem, case tag, logo) is drawn by `ShareCardRenderer.kt` at fixed coordinates (`CardLayout`, native 1024x1536, identical across all 5 rarities) uniformly scaled per `Format`. Iterated with the director through 16 rounds of a Python/PIL mockup (`sim_tcg_v5.py`-`v16.py`, not checked into the repo) before Kotlin porting, since this sandbox has no JVM/Android runtime to run real Canvas code. Confirmed design adds: rarity-colored chip badges on all 5 rarities (HIDDEN gets a gradient+sparkle version); EPIC/LEGENDARY emblem sparkle/glow escalation; a shrunk, less-cropped art window; TCG-style foil title text (glow+outline+gradient fill, vertical gradient for EPIC/LEGENDARY after a horizontal gradient was found to make some letters nearly invisible on light panels, HIDDEN gets a full horizontal rainbow since its dark panel is always safe); and a bespoke HIDDEN emblem (double glow ring, orbiting dots, glowing "?" glyph) replacing the plain gold "?". `docs/CARD_LAYOUT_SPEC.md` updated to the true final v16 coordinate/design spec (was a stale v13 snapshot). Found and fixed a real Kotlin scoping bug while porting: `Paint(...).apply{}` blocks where a parameter/local name collided with a Paint property name (color/alpha/strokeWidth/shader) silently resolved the RHS to the receiver's own value instead of the intended argument -- renamed every colliding identifier project-wide in this file. Verified via brace/paren balance check and full manual code review (no local kotlinc/JVM available); on-device visual confirmation still pending (same standing gap as v0.36 item 6).
| 0.38.0 | 38 | pending | development | Fixed 8 real-device feedback items from a v0.37 APK install, plus one bonus bug found via the director's screenshots. (1) Share button silently saved a permanent copy to the public Pictures gallery via MediaStore before the share sheet even opened -- switched to a cache-only file handed to the share sheet via a newly-added FileProvider (androidx.core added as this app's first AndroidX dependency), so nothing is saved unless the user explicitly picks a save target inside the OS share sheet. (2) Modals could scroll their close button out of view when content was tall -- restructured `.sheet` as a flex column with a scrollable `.sheet-scroll` middle region, keeping the handle and close button always in view. (3) Real regression from v0.37: the share card's logo was hardcoded to the Korean wordmark regardless of the in-app language, confirmed via a director screenshot showing a Japanese card with a Korean logo -- plumbed the `lang` param through to the logo lookup. (4) MainActivity opted every API 30+ device into edge-to-edge (transparent status/nav bars) even though only Android 15+ (API 35, this app's targetSdk) actually forces it -- now only forces edge-to-edge where unavoidable, keeping the theme's opaque bars everywhere else. (5) The v0.36 card-shine fix removed the wiper back-and-forth but kept `ease-in-out` timing on the sweep, which still read as an artificial slow wind-up -- switched to `ease-out` with a shorter, snappier sweep (18% vs 35% of a now-6s cycle). (6) `--muted` (used for nearly every secondary text label app-wide) measured 3.36-3.8:1 contrast, below WCAG AA's 4.5:1 -- darkened to `#6B5F54` (5.5:1+) and bumped several of the smallest (10-11px) labels by 1px. (7) Switching tabs (or opening/closing the full-screen incident detail) kept the previous scroll position, showing blank space under short content -- added `window.scrollTo(0,0)` to `setTab()`/`openDetail()`/`closeDetail()`. (8) Tapping an archive card left a lingering WebView tap-highlight overlay -- disabled `-webkit-tap-highlight-color` app-wide. Bonus fix: the incident detail page's ESCAPE_FAILED stats showed the raw `windowMs` JS key instead of a translated label (metricLabel()'s default fallback), added a proper case. Verified via Playwright (393x851): tap-highlight transparent, no horizontal overflow, scroll resets to 0 across tab/detail navigation, sheet-close always within viewport, windowMs label renders correctly, settings About string updated, and the shine keyframe/timing values confirmed directly from the live CSSOM. The Kotlin-side changes (FileProvider, edge-to-edge branching, lang-aware logo) again rely on CI for actual compilation, same standing gap as this renderer's prior versions.
| 0.39.0 | 39 | pending | development | Replaced the home screen's EPIC/LEGENDARY card shine effect with a prototype the director built themselves. v0.38's fix only tuned timing (ease-in-out to ease-out, sweep ratio/cycle length) but left the actual shape unnaturally boxy -- a rotated fixed-width DIV with a flat vertical gradient. The director recorded v0.38's own confirmation video, ran it through another AI tool to reconstruct a base frame plus a new CSS technique, and sent back a zip (card-shine.css/card_shine_soft.mp4/card_preview_base.png/README.txt). Verified the technique genuinely works via pixel-diffing animation frames against a static baseline (the effect's peak alpha is only 0.18, too subtle to see by eye in a compressed video). Ported it in: a single `::after` pseudo-element on `.card` itself with an already-diagonal (108deg) gradient that fades in/out while sliding (4.8s linear cycle), replacing the old rotated-box-plus-two-child-divs approach entirely -- no more hard rectangular silhouette. Dropped the old per-rarity purple/gold tinting in favor of a plain white sheen (real holo/glass reflections are typically white regardless of the card's own color) and added `prefers-reduced-motion` support the old version lacked. Re-verified text legibility across multiple animation phases via Playwright (the new technique has no clip-path keeping it off the text column, unlike the old one -- confirmed safe given the very low peak alpha) plus class wiring, keyframe values, reduced-motion behavior, and no layout overflow.
| 0.40.0 | 40 | pending | development | Root-cause fix for the card shine still reading as "a line" after v0.39 ported the director's own prototype CSS as-is. First tried blur (16px), but pixel-scanning the gradient in isolation (against a flat background, across every translateX offset) showed the edges were already smooth (max 2/255 jump between adjacent pixels) -- blur was solving a problem that didn't exist. The real cause: the gradient's transparent-to-peak-to-transparent transition was crammed into only 14% (43%-57%) of the gradient line, which is fundamentally a thin stroke shape no matter how soft its edges are (confirmed visually via 6x contrast amplification -- unmistakably a diagonal line). Widened the transition to span the full 0%-100% of the gradient so brightness builds and fades gradually across nearly the whole sweep, eliminating the "band" shape entirely; re-verified via the same isolated pixel-scan technique that it now reads as a broad soft diagonal glow. Dropped the now-unnecessary blur (also sidesteps a real-time blur filter on an oversized element as a potential real-device performance risk). Re-confirmed text legibility across multiple animation phases on the actual card, lowering peak alpha slightly (0.18 to 0.15) since more of the card is lit simultaneously with the wider span.
| 0.41.0 | 41 | pending | development | Replaced three failed CSS-based card-shine attempts (v0.38-v0.40, all eventually read as "a line" on a real device) with a Canvas 2D holofoil effect confined entirely to the art window's own pixels, adapted from a prototype the director built (referencing poke-holo.simey.me's approach): a shared virtual light drives a rainbow diffraction layer, an elliptical clear-coat glare, stationary sparkle grains, and a shifting rim light. Structurally cannot touch card text (it's a separate canvas element inside .scene, not an overlay spanning the card), resolving the root cause of all three prior CSS attempts at once. The original prototype hardcoded its masks to one specific 380x326 screenshot (a moon-glow boost box, a "protect the cat's face" cutout) -- generalized by sizing the canvas to .scene's actual rendered box per card and dropping the illustration-specific hardcoded regions in favor of a generic radial falloff. Found and fixed a real crash via testing: the prototype's getImageData()-based luminance mask throws a "tainted by cross-origin data" SecurityError on any canvas with a file://-loaded image drawn into it (reproduced via Playwright using the same file:// loading this WebView uses) -- removed all getImageData/putImageData calls from the file entirely, replacing the luminance mask with a uniform one since every remaining operation (drawImage/gradients/compositing) is a write-only operation Chromium never blocks regardless of image origin. Added render()-level lifecycle management (stopHolo()/mountHolo()) so the animation loop is cancelled on every re-render and self-terminates if its canvas leaves the DOM, preventing orphaned requestAnimationFrame loops from accumulating across tab switches; respects prefers-reduced-motion by never mounting the effect. Verified via Playwright: exactly one canvas mounts only for an EPIC/LEGENDARY featured card, zero console errors (previously threw a SecurityError per card before the getImageData fix), no leak across 5 tab-switch cycles, and screenshots across both EPIC and LEGENDARY confirm the shimmer/glare/sparkle stay confined to the art window with text fully legible throughout.
| 0.42.0 | 42 | pending | development | Fixed 3 issues found by running the /code-review skill against v0.41's new holofoil canvas code. (1) createCardHolo()'s base draw stretched the source illustration to fill the canvas with drawImage(image,0,0,W,H), ignoring aspect ratio, while the .scene element right underneath it uses background-size:cover -- every shipped illustration is a ~1.78:1 landscape photo against a near-square art box, so the canvas's opaque per-frame redraw was visibly squashing the artwork on every single EPIC/LEGENDARY card (not an edge case, since all 14 shipped incident types hit this path). Replaced with cover's own math (scale by the larger ratio, center-crop) so the canvas draws the identical crop the CSS background beneath it shows. (2) The canvas backing store was sized to plain CSS pixels while stretched to the same box via width:100%/height:100%, so on a real ~2.5-3x devicePixelRatio Android screen the browser upsampled a low-res buffer, making the holofoil layer visibly blurrier than the crisp content around it -- sized the backing store to CSS-size*devicePixelRatio instead, with the function's few absolute-pixel constants (border radius, stroke widths, flake size) scaled by the same factor. (3) Confirmed via CSS stacking rules that the absolutely-positioned .holo-canvas would always paint above the non-positioned .moni-asset flex item regardless of DOM order, which would fully hide the MONI character illustration if the currently-dead 'overlay' render mode is ever reached again (all 12 non-HIDDEN incident types currently resolve to 'scene', so this path isn't reachable today) -- added an explicit z-index to .moni-asset as a defensive fix. Re-verified the full v0.41 regression suite (canvas count, console errors, tab-cycle leak check, reduced-motion) plus new checks (canvas backing-store resolution at deviceScaleFactor 3, illustration aspect ratio preserved) via Playwright.
| 0.43.0 | 43 | pending | development | Fixed 5 real-device feedback items from a v0.42.0 APK install. (1/2/4) The hardware back button always exited the app -- even right after opening it, and from the incident detail page -- because this is a single-page WebView app that never performs a real navigation (tabs/detail page/archive modal are all just JS state mutations on the same file:// URL), so MainActivity's webView.canGoBack() check was structurally always false and every back press fell through to finishing the Activity. Added window.onNativeBackPressed() to index.html, which closes exactly one open layer at a time in stacking order (the #overlay modal, then the detail page, then a non-home tab) and reports whether it handled the press; MainActivity.onBackPressed() now calls it via evaluateJavascript and only exits when it reports nothing was left to close. (3) There was no data-reset feature anywhere -- turned the previously-inert "데이터 관리" settings row into a real action gated behind a confirmation sheet (reusing the archive-detail modal's #overlay pattern), and added NativeBridge.resetAllData()/DiscoveryRepository.reset() so all three copies of "what's been discovered" (JS localStorage, the native backup file, and DiscoveryRepository's SharedPreferences) get cleared together instead of drifting out of sync. (5) General settings-screen unresponsiveness, cited as many buttons not reacting to taps: partly because Theme/Data/Help/About rows genuinely had no onclick handler at all (now real: Data opens the reset sheet, Help/About open info sheets, Theme at least shows a toast since there's nothing to switch to yet), and partly an app-wide root cause -- `-webkit-tap-highlight-color` is disabled globally (v0.38) but most buttons had no `:active` state of their own to replace it with, so taps gave zero visual feedback until the next render landed; added `:active{transform:scale(.97)}` feedback to every primary tap target (settings rows, case rows, archive cards, the featured card, icon buttons, the primary button, tab buttons, sheet buttons), matching the one element that already had this (`.share`). Verified via Playwright: all 4 back-press scenarios (overlay open, detail open, non-home tab, nothing open) return the right handled/not-handled result and close exactly one layer each; all 7 settings rows now render with a chevron and no `static` class; the reset flow clears seeded fake discovery data from localStorage end to end; help/about/reset sheets render with the correct version string and zero console errors.
| 0.44.0 | 44 | pending | development | Follow-up to v0.43's back-button fix per director feedback: reaching the home tab with nothing left to close still exited immediately on the next back press with no confirmation. Replaced that "nothing to close, so exit" path in window.onNativeBackPressed() with openExitConfirm(), a "정말 종료하시겠습니까?" (are you sure you want to exit?) sheet using the same #overlay pattern as the reset-confirm/help/about sheets; the function now always returns true, so MainActivity no longer ever exits the Activity on its own -- the sheet's own "종료" button calls a new NativeBridge.exitApp() (Activity.finish()) once the user actually confirms. A second back press while the sheet is open hits the existing #overlay-close branch first and just dismisses it (cancel), matching how a native AlertDialog's back-button behavior dismisses rather than confirms. Used a new non-red `.confirm` button style (matching `.primary`'s purple) rather than the reset sheet's `.danger` red, since exiting loses no data and shouldn't read as equally alarming. Verified via Playwright: back press on the home tab with nothing open shows the exit-confirm sheet and returns true; a second back press while it's open closes it and returns true without reaching N.exitApp(); clicking cancel closes it; clicking confirm with N undefined (preview mode) is a safe no-op; zero console errors.
| 0.45.0 | 45 | pending | development | Self-audit prompted by the director asking "anything else to do?" -- re-checked whether v0.43's "fix the back button throughout" request actually covered the whole app and found 2 remaining gaps (not new director feedback). (1) The two-screen onboarding flow was missed entirely: none of onNativeBackPressed()'s three checks (#overlay/detailItem/tab) ever fire during onboarding since they're all at their defaults, so back on the second onboarding screen fell straight through to v0.44's exit-confirm sheet instead of returning to the first screen. Added a check at the very top of the function (onboarding fully replaces #app, so it takes priority) that returns to step 1 when on step 2. (2) The header's language-toggle badge (KO/JA) had no `:active` press feedback -- same root cause as item 5's settings-button complaint (no visual response on tap despite the onclick handler working fine) just outside the settings screen. Added `.lang` to the shared `:active{transform:scale(.97)}` rule from v0.43. Verified via Playwright: without the `onboarding=0` skip param (matching a real first launch), back on the second onboarding screen returns to the first without showing the exit-confirm sheet, and a second back press from there does show it; re-ran the v0.44 exit-confirm regression suite with no change in behavior; zero console errors.
| 0.46.0 | 46 | pending | development | Director feedback: language switching existed in two identical places -- the header's KO/JA badge (shown on every screen) and the settings language row -- both calling the same cycleLang(). Confirmed which to keep (settings) and removed the header badge along with the now-dead `.lang` CSS rule and its v0.45 `:active` press-feedback entry; cycleLang() itself is untouched, still called from the settings row. Verified via Playwright: `.lang` no longer exists in the DOM, the settings language row still switches ko/ja correctly, and `.brand`'s layout holds up without the removed element; zero console errors.
| 0.47.0 | 47 | pending | development | Director said "build whatever's needed" after a proposed list of 6 improvements; implemented 5 (the 6th, real-device confirmation of HIDDEN-rarity rendering, isn't something code can do). (1) Records tab gained a "last 7 days" bar recap -- state.history.days has been persisted daily since v0.35 but nothing ever displayed it; days with no stored entry render as a distinct greyed bar rather than a 0-height one. (2) Added a data-export settings row (NativeBridge.exportBackup()) pairing with v0.43's reset -- writes the same {settings,history} payload to a cache file and hands it to the OS share sheet via FileProvider, same mechanism as card sharing. (3) Fixed a known ShareCardRenderer gap (title never wrapped, could overflow the header panel on a long incident name) by shrinking the font size to fit instead of wrapping (the header panel isn't tall enough for two lines), floored at 28f. (4) Added a privacy-policy draft sheet in settings -- an honest description of the app's actual data handling (verified against this session's own code reading), explicitly marked as unreviewed with bracketed placeholders for contact/date that must be filled before any store submission. (5) Implemented the daily-reminder notification the app's POST_NOTIFICATIONS permission had been requesting since v0.24 with nothing to use it for: new ReminderScheduler (AlarmManager.setInexactRepeating, no WorkManager dependency or exact-alarm permission needed) + DailyReminderReceiver + BootReceiver (re-arms after reboot, since AlarmManager alarms don't survive one), toggled from a new settings row; the notification's language follows the in-app language setting (mirrored into native SharedPreferences via a new NativeBridge.setLanguage(), called whenever index.html's language changes) rather than the device locale, since the receiver has no other way to read WebView-side state. Verified via Playwright: week-recap bar heights/empty-day styling/total are correct against seeded fake data, all 10 settings rows render in order, the reminder toggle flips state.settings.dailyReminder safely with N undefined, export produces the correct JSON shape, the privacy sheet renders with its placeholders, and the full v0.43-v0.46 back-button/reset/settings regression suite still passes; zero console errors. The three new Android-framework-dependent Kotlin files (AlarmManager/NotificationManager/BroadcastReceiver) can't be exercised by this session's JVM-only core-module test harness, so as with all Kotlin changes this session, real compilation and on-device notification behavior depend on CI and a real-device check.
| 0.48.0 | 48 | pending | development | Two real-device regressions reported after installing v0.47.0. (1) The hardware back button still just exited the app despite v0.43-v0.45's fix -- root cause: this session's Playwright tests for that fix only ever called window.onNativeBackPressed() directly in a browser, never exercising the real native dispatch path (Activity.onBackPressed()), which headless Chromium has no way to simulate at all. The actual bug: onBackPressed() is deprecated from API 33 onward, and with enableOnBackInvokedCallback="true" (v0.36) plus this app's targetSdk 36, the platform's documented "still calls onBackPressed() for compatibility" fallback wasn't reliable enough in practice. Fixed by registering a real OnBackInvokedCallback on API 33+ in MainActivity.onCreate() (the officially correct approach for a predictive-back-opted-in app), extracting the shared logic into handleBackPress() so both that callback and the now API-29-32-only onBackPressed() override call the same code; super.onBackPressed() calls were unified to finish() (equivalent for this Activity, which has no fragment back stack, and the only option available from the callback path). (2) The screen kept scrolling/bouncing even on short content (e.g. the incident detail page) -- Android WebView's default overscroll edge-glow effect, a View-level behavior CSS can't reach; fixed with webView.overScrollMode = View.OVER_SCROLL_NEVER, plus a defensive overscroll-behavior:none in CSS for Chromium's own scroll-chaining. Neither fix could be verified by this session's tooling (no Android OS back-dispatch or WebView overscroll rendering in headless Chromium) -- both depend entirely on the next real-device check.
| 0.49.0 | 49 | pending | development | Director feedback: adjust proportions/spacing against well-known (Korean) apps' conventions, without changing the UI itself. Narrowed down to 3 concrete issues. (1) The home screen's 4-column daily-strip (opens/usage/unlocks/cases) split space evenly, but the usage-time cell's actual content (fmtMs(), e.g. "2시간 23분") is 3-4x longer than its siblings' 1-2 digit numbers -- measured via Playwright that it alone wrapped to 2 lines while the other 3 stayed on 1, visibly unbalancing the row. Rebalanced the columns to 0.85fr 1.5fr 0.85fr 0.7fr and added white-space:nowrap; confirmed all 4 cells stay single-line and non-overflowing at 360/390/412px (common Korean Android phone widths). (2) Side margins (18px/14px) and gaps between stacked cards/rows (8-10px) were tighter than well-known Korean apps' actual numbers (KakaoTalk ~16px sides, Toss ~20px, Danggeun ~16px) -- bumped to 20px/16px sides and 10-12px gaps; no layout structure or card sizing changed, only the spacing values. (3) Font sizes in the code itself looked fine on inspection, but Android WebView scales all text by the device's system font-size (accessibility) setting while PNG/emoji icons don't scale with it -- on any device where that setting isn't 100% (a common accessibility choice), this app's intended text-to-icon ratio silently breaks. Pinned WebView.settings.textZoom to 100 in MainActivity so the app always renders its own intended sizes regardless of that per-device setting. Verified via Playwright at 360/390/412px that the daily-strip fix holds and screenshotted the updated spacing across home/records/archive/settings; re-ran the full v0.43-v0.48 back-button/reset/reminder/export regression suite with no change in behavior. The textZoom fix is an Android WebView accessibility behavior this session's tooling can't exercise -- needs a real-device check with the system font size changed.
| 0.50.0 | 50 | pending | development | Two more real-device issues from v0.49.0 screenshots. (1) The incident detail page "still scrolls" -- v0.48's overscroll fix addressed a different hypothesis (bounce/glow on short content) and this turned out to be a real, measurable height bug instead: render()'s detail-mode branch never renders tabs() (the bottom tab bar), but `.app`'s base rule still reserves `calc(90px + safe-area-inset-bottom)` of padding-bottom for it regardless, stacked on top of `.detail-page`'s own already-adequate bottom padding. Measured via Playwright that zeroing this out for detail-mode drops actual content height from 736px to 646px, comfortably fitting even a tight 700px effective viewport (real status bar + gesture nav chrome eats into a phone's usable height more than a plain viewport height suggests) -- added `.app.detail-mode{padding-bottom:0}`. (2) The featured card's illustration occasionally renders as scrambled horizontal color bands instead of the real art. Nothing in createCardHolo()'s draw logic explains that shape of corruption, and the image is only ever drawn after its onload fires, so the likely cause is a real (if hard to pin down without the device) Android WebView/GPU driver quirk where a freshly-resized canvas backing store briefly shows stale GPU texture memory instead of the spec-required transparent-black before anything paints into it. Added an explicit clearRect() immediately after the resize in mountHolo() -- a no-op on spec-compliant browsers, a cheap mitigation if that's the real cause. Verified via Playwright that detail-page height now fits at 844/780/700px viewport heights and the existing back-button/exit-confirm regression suite still passes; the GPU-driver-specific corruption can't be reproduced or confirmed fixed in headless Chromium at all, so this one specifically needs a real-device recheck across several card opens.
| 0.51.0 | 51 | pending | development | Director gave 8 pieces of feedback at once; implemented the 5 that were directly actionable in code (icon/asset unification needs new art the director generates, two items were too ambiguous to act on without confirming, and one is a design tradeoff -- see the dated entry in docs/DEVELOPMENT_HISTORY.md for details on what's held). (1) "Top apps" list showed the raw package name's last segment (e.g. "talk" for com.kakao.talk) -- added NativeBridge.appLabel() resolving the real installed-app name via PackageManager, with compactPkg() falling back to the old behavior on lookup failure; fixes both the records-tab list and the incident card's package pill since both already share that function. (2) Unlock count never registered anything -- SessionBuilder only trusted EventType.UNLOCK (Android's KEYGUARD_HIDDEN), which multiple real-device/OEM reports (Samsung especially) confirm is unreliably or never surfaced through queryEvents() at all; added SCREEN_ON as an equally-valid unlock-proxy signal, verified with a standalone JVM check that unlockStarted now correctly flips true from SCREEN_ON alone with zero UNLOCK events, and stays false with no signal at all. (3) LEGENDARY rarity fired far too easily, flooding daily reports -- REENTRY's `count >= 3 && avg <= 10_000` LEGENDARY branch matched completely ordinary phone behavior; raised to `count >= 10 && avg <= 5_000`. Verifying this via the JVM core-module test harness surfaced a real interaction: demoting REENTRY out of the uncapped LEGENDARY/HIDDEN tier moved it into resolve()'s top-3-ordinary-incidents competition, which then correctly bumped a weaker QUICK_EXIT out of an existing smoke test's fixed scenario -- not a regression, just resolve()'s existing "most notable 3 per day" cap working as designed once the threshold was fixed; updated the smoke test's assertion and comment to match. (4) Added a user-configurable reminder time (previously fixed at 9pm) via a settings sheet with a native `<input type="time">` (renders Android's own time-picker dialog, no custom UI needed) -- NativeBridge.scheduleDailyReminder() now takes hour/minute, persisted so BootReceiver's reschedule keeps using the last-chosen time. (5) The home screen's hero banner text was static every single day -- replaced with a heroGreeting() that reacts to today's actual report (distinct messages for an empty day, a HIDDEN find, or 2+ LEGENDARY cards) and otherwise rotates through a 5-message pool keyed by calendar day. Verified via Playwright: app-name resolution's JS fallback logic, all 4 heroGreeting scenarios, the reminder sheet's time input/save/turn-off flow, and the full existing back-button/reset/export regression suite; zero console errors. The unlock-signal and rarity-threshold fixes were verified for real via the session's JVM-only core-module test harness (pure Kotlin, no Android dependency) rather than just checked for syntax.
| 0.52.0 | 52 | pending | development | Followed up on 3 of the 4 items held from v0.51 after the director clarified direction (the 4th, same-type-different-app duplicate cards, was confirmed to leave alone). (1) Settings icon unification -- not a code change, but a new section in docs/GPT_IMAGE_PROMPTS.md with 7 ready-to-paste prompts (theme/data/export/help/privacy/reminder/about), matching the existing icon set's actual style (inspected icon_language.png/icon_notification.png directly: bold black outline, white fill, transparent background, thin white sticker-style border, ~128-145px square) via a shared style boilerplate, same pattern as the incident-illustration prompts doc. (2) Archive was showing every non-HIDDEN incident type's real name/art even when never discovered -- the "locked/???" placeholder was only gated on the two HIDDEN-rarity types (`x.hidden && !foundAny`), so all 12 regular types spoiled themselves immediately, even self-labeling "발견됨" (Found) via a metrics-fallback despite genuinely never being found. Simplified the lock condition to `!foundAny` so every undiscovered type gets the same "???" treatment; verified via Playwright against demo data that all 4 undiscovered types flip to "???" while the 8 discovered + 1 HIDDEN stay revealed. (3) Added an interactive, Pokemon-TCG-Pocket-style card tilt to the home screen's featured EPIC/LEGENDARY holofoil effect -- createCardHolo()'s render() already accepted an explicit light-position override, just never had an input source; wired pointer-drag on the art window to feed real drag position into it (touch-action:none while dragging, and an 8px movement threshold that distinguishes an actual drag -- which suppresses the click that would otherwise open the detail page -- from a normal tap, which still opens it as before). Verified via Playwright with simulated mouse drags: a real drag doesn't navigate to the detail page, a plain tap still does, and a pixel-diff between two drag positions confirms 43% of the art-window's pixels genuinely change with drag direction. Full existing back-button/reset/reminder/export regression suite re-confirmed; zero console errors.
| 0.53.0 | 53 | pending | development | Director correction on v0.52's Pocket-style interactive holofoil effect: it belonged on the "card screen" opened by tapping a home-screen incident, not on the home screen itself, and that detail page's plain white-background/differently-colored-middle-layer look ("가장 거슬렸어" -- flagged as top priority) should instead feel like the share card. (1) Full redesign of the incident detail page: replaced the old `.detail-page{background:var(--bg) !important}` (forcing every rarity back to the app's plain light theme) with a full-bleed atmospheric backdrop reusing the exact same `backgrounds/share/1080x1920/bg_<rarity>_vertical.png` art the share PNG uses, behind a bordered "TCG card" component (`.tcg-card`) reusing the exact same `frame_bg/<rarity>.webp`/`medallions/<rarity>.webp` assets and CardStyle.kt's per-rarity palette (ported 1:1 into new `tcgPalette()`/`foilTitleStyle()` JS functions, including the EPIC/LEGENDARY/HIDDEN gradient-fill "foil" title treatment), with header/art/info/emblem regions positioned via CardLayout's own pixel rects converted to %. (2) Moved the interactive drag-tilt holofoil canvas (createCardHolo()/mountHolo(), v0.41-v0.52) off the home screen's featured card entirely and onto this redesigned detail page's art window instead -- mountHolo() needed no changes (it was already written generically against whatever `.holo-canvas` exists), only the call site in render() and the canvas's insertion point in card()/detailPage() moved; the EPIC/LEGENDARY-only gating from v0.27 is preserved. (3) First attempt at recoloring the back/share icon buttons for the dark backdrop (a dark chip + brightness(0) invert(1) on the glyph) erased the arrow entirely, since icon_back.png/icon_share.png are two-tone (white circle + black glyph) assets, not a single-color glyph on transparent -- reverted to the plain white `.icon-btn` used everywhere else in the app, which reads fine floating over the dark backdrop as-is. Verified via Playwright: all 5 rarities' redesigned detail pages screenshot correctly with the right backdrop/frame/holo-canvas presence, a simulated drag on an EPIC card's art window tilts the hologram with zero console/canvas-taint errors, the home screen's featured card no longer carries a `.holo-canvas`, and the back-button-closes-detail-page path (`window.onNativeBackPressed()`) plus the full existing v0.43-v0.52 regression suite (reset/reminder/export) still pass with zero console errors.
| 0.54.0 | 54 | pending | development | Three more rounds of feedback on v0.53's TCG-card detail page redesign, tested back-to-back on the real device. (1) "카드 자체가 움직이지는 않네" (the card itself doesn't move) -- dragging only ever moved the holo canvas's internal light position; added an actual 3D tilt (rotateX/rotateY, up to 14deg) on the whole `.tcg-card`, driven by the same drag input, with a `.tilting` class that drops the snap-back transition while actively dragging so it tracks the finger with no lag. (2) "배경도 검은 배경이네;; 그냥 저 고양이발바닥 무늬 배경으로 하지" (the backdrop's dark too, just use the paw-print background) -- swapped the dark `backgrounds/share/*/bg_<rarity>_vertical.png` + black scrim backdrop for `cardPatternBg(i)`, the exact same light warm paw-print texture the home card's own `.card-pattern` already uses (HIDDEN's own variant of that asset is already a moody dark scene on its own, so one swap handles both moods with no branching). New `--page-text`/`--page-muted`/`--page-panel`/`--page-panel-line`/`--page-stat-line` custom properties (set inline per rarity) now drive the outer case-id/sub/stats colors, since the pre-existing `.hidden`/`.normal` etc. classes' own background+color rules would otherwise win this page's cascade on selector specificity regardless of source order. (3) "스크롤 되네? 어떻게든 한 화면에 담아주지" (it still scrolls, fit it on one screen) -- `.app.detail-mode`/`.detail-page` are now a fixed-height (`100dvh`, `100vh` fallback), non-scrolling flex column with the card's wrap as the flexible middle; a new `fitTcgCard()` function measures that wrap's actual laid-out height on every render and shrinks (never grows past its usual 360px cap) `.tcg-card`'s width to whatever keeps its 1024:1536 aspect ratio inside what's left, instead of the fixed max-width that could make the card (and so the whole page) taller than a short screen. Three real bugs found and fixed while building this: (a) a v0.54 code comment abbreviated a path as `backgrounds/share/*/bg_<rarity>_vertical.png` -- the embedded `/*` happened to pair with the CSS comment's own opening `/*` to read as an early `*/`, silently truncating that comment and corrupting everything after it into invalid CSS, which the parser then discarded along with roughly 40% of the entire stylesheet (verified by counting `document.styleSheets` rules: 137 actually parsed vs 200+ expected) -- same class of bug as v0.47's AndroidManifest.xml `--`-in-comment issue; fixed by spelling out the path instead of using a wildcard, and added a rule-count sanity check to this session's CSS-editing routine going forward. (b) `.tcg-card-wrap{flex:1 1 auto}` created a circular sizing dependency (the wrap's hypothetical size came from its child's aspect-ratio-driven height, which came from the child's width, which came from the wrap) that converged to an absurdly small ~60px card height -- fixed by using `flex:1` (flex-basis:0) so the wrap's size depends only on real leftover space in `.detail-page`, not its own child. (c) after fixing (a) and (b), HIDDEN's backdrop still rendered as the light theme instead of its own dark variant -- v0.53's explicit dark `#0A0A0C` background on `.app.detail-mode` had been incidentally masking `body`'s cream background everywhere; removing it in this version's background swap left `.detail-backdrop` (`position:fixed;z-index:-2`) with no local stacking context, so it sank below body's own unpositioned background at the document root instead of just behind `.detail-page`; fixed with `isolation:isolate` on `.app.detail-mode`. Verified via Playwright across all 5 rarities at both a normal (393x851) and a short/older-phone (360x640) viewport: `document.body.scrollHeight` now equals `window.innerHeight` exactly (no scroll) in every case, the backdrop pattern image renders correctly for every rarity, a simulated drag on an EPIC card's art window produces a real `matrix3d(...)` transform on `.tcg-card` that resets to `none` on release, and the full existing back-button/home-screen regression suite still passes with zero console errors.
| 0.55.0 | 55 | pending | development | Director feedback: "또 알림 허용 하는거 누를때 이 앱이 어디에 있는지 표시하는 기능도 있음 좋겠내" -- wording alone was ambiguous between three different permission/settings flows, so a clarifying question confirmed it's about the USAGE ACCESS permission specifically. `N.openUsageSettings()` opens `Settings.ACTION_USAGE_ACCESS_SETTINGS`, a system screen listing every installed app with no indication of which row is this one -- genuinely hard to find "또 열었네?" in a long device app list. Two-part fix in `NativeBridge.openUsageSettings()`: (1) the intent now carries a `package:<our package>` data URI, which some Android versions/OEMs honor for this action to jump straight to this app's own toggle row instead of the full list (undocumented behavior several apps already rely on; wrapped in `runCatching` so a device with no activity for that data URI falls back to the plain list-only intent automatically); (2) either way, a native `Toast` (survives the switch away from the WebView into Settings, unlike an in-page HTML toast) names the exact app label to look for, so devices where (1) doesn't jump directly still tell the user what they're hunting for -- the toast's language mirrors the same native `SharedPreferences("app_prefs")` flag `DailyReminderReceiver`'s notification text already reads, kept in sync by the existing `setLanguage()`. This session's tooling can't exercise Android-framework classes (Toast/Settings/Uri) outside a real compile, so this is verified by code review + CI compiling successfully; whether the direct jump actually works (and how it varies by OEM) needs a real-device check.
| 0.56.0 | 56 | pending | development | Real-device regression reported after installing v0.55.0: "처음 시작할때 사용정보 접근 허용을 눌러도 그 화면으로 안가고 앱이 시작되고 다시한번 허용을 눌러야되는 불편함" (on first launch, tapping "allow usage access" doesn't navigate to that screen -- the app just continues, and a second tap is needed). Root cause: v0.55's `package:` data URI on the `ACTION_USAGE_ACCESS_SETTINGS` intent (an attempt to jump straight to this app's own row instead of the full app list) was wrapped in `runCatching` to fall back to the plain intent on failure, but `startActivity()` doesn't throw just because the resolved activity doesn't actually do anything useful with that data URI -- on the director's device the intent silently resolved to something that immediately returned control to the app without ever showing usage access settings, so the fallback never triggered and the first tap looked like a no-op. Reverted `NativeBridge.openUsageSettings()` to the plain, universally-supported `Settings.ACTION_USAGE_ACCESS_SETTINGS` intent with no data URI -- reliability over an undocumented, OEM-dependent shortcut that wasn't reliable anyway. Kept the other half of v0.55 (the native Toast naming "또 열었네?" to look for in the list), since that part is unconditionally safe regardless of whether a direct jump ever happens. Verified by code review + CI compile only, as with v0.55 -- the reverted line matches what shipped reliably through v0.43-v0.54, so risk is low; the real-device improvement still needs confirmation.
| 0.57.0 | 57 | pending | development | Director sent two screenshots (onboarding's "알림 허용" notification-permission screen, then the home tab's separate "모니가 사건을 조사하려면" usage-access gate) with "첫번째화면에서 눌렀는데 두번째 사진에서도 또 눌러야된다는거야 그냥 통합하면되는데" (tapped the button on the first screen, but still had to tap again on the next one -- just merge them) -- a distinct issue from v0.55/v0.56's "find the app in the usage-access list" feedback: these are two fully separate permission screens the user has to separately notice and tap. Android has no single dialog that grants both notification permission (a runtime dialog) and usage access (only grantable from a Settings page) at once, so this can't become one literal tap -- instead, `onboardFinish()` now checks `N.hasUsageAccess()` when onboarding finishes and, if not yet granted, immediately chains into `N.openUsageSettings()` (the same native flow + guidance toast the settings row's button already uses) regardless of whether the notification button tapped was "허용" or "나중에" (usage access is required for the app to function at all, unlike the optional notification permission), so the user continues straight from onboarding into granting it instead of landing on the home tab's own separate gate and having to find and tap a second button there. Already-granted access (e.g. a relaunch) correctly skips the extra call. Verified via Playwright with a `window.OpenedAgainNative` stub injected via `addInitScript()` (before the page's own script captures it into its `const N` at parse time -- an initial attempt using `window.N` after page load silently no-op'd since that's the wrong global name, giving a false pass until caught) across three cases: usage access not yet granted + "허용" tapped (both notification request and openUsageSettings fire), already granted + "나중에" tapped (neither fires again), and preview mode with no native bridge at all (no throw, onboarding still completes) -- plus the full existing detail-page/back-button regression suite, zero console errors.
| 0.58.0 | 58 | pending | development | Director asked for more after v0.55/v0.56's name-only Toast guidance: "수많은 앱들중에 우리 앱이 어디있는지 알려주는 것까지 기능으로 넣으면 좋겟지" (with so many apps in the list, show where ours actually is). Investigated what's actually possible: Android's app sandboxing means this app has no way to draw a highlight/arrow over a specific row inside the Settings app's own screen -- that would require SYSTEM_ALERT_WINDOW (draw-over-other-apps) plus an AccessibilityService just to locate the row, both disproportionately heavy and privacy-sensitive for this one feature, against Play Store policy for non-accessibility use of accessibility services, and directly contradictory to v0.55-v0.57's own goal of asking for fewer permissions, not one more. Treated this as a real platform ceiling rather than something to work around with heavyweight permissions. Within that ceiling, improved what's still possible: `NativeBridge.openUsageSettings()`'s guidance Toast now also mentions using the search icon when present -- several Android skins (including Samsung One UI's "특별 접근"/Special access list) do put a search feature on this exact screen, which finds the app far faster than scanning an unsorted list by eye, and is a harmless no-op mention on skins/versions that don't have one. Verified by code review + CI compile only (a string-only native change); told the director plainly that a true "point at the row" feature isn't achievable without adding another permission ask, which would undercut the whole point of this feedback thread.
| 0.59.0 | 59 | pending | development | Director confirmed the v0.54 card tilt works on the real device, but only when dragging over the illustration ("일러스트있는 부분을 드래그해야 움직이는 것을 확인했어") and asked for the whole card to be draggable with the prism/holo effect covering all of it, not just the art window. Root cause: both the drag-detection area and the holofoil canvas were still confined to `.tcg-art`. Structural fix: `.holo-canvas` moved from a child of `.tcg-art` to the last child of `.tcg-card` itself -- `mountHolo()`'s drag/tilt logic already operated generically on `scene` (=`canvas.parentElement`), so moving where the canvas mounts alone extends both the drag-detection area and the canvas's own bounds to the whole card, no JS logic changes needed there. This required reworking `createCardHolo()`'s rendering algorithm, though: it used to redraw a cover-fit crop of the incident photo directly into the canvas and then composite the shimmer layers on top of that copy using canvas-internal color/screen blends -- with the canvas now spanning the whole card (frame art + header panel + art photo + info panel + medallion, not one photo this function could redraw), reproducing that composite would mean reimplementing the entire card's rendering in Canvas 2D. Instead the canvas is now a fully transparent overlay that draws nothing but the shimmer layers (rainbow diffraction, glare, sparkles, rim light) with plain source-over compositing, and CSS `mix-blend-mode:screen` on the canvas element lets the browser blend those layers with whatever real card content sits behind it -- the effect's two most prominent layers already used 'screen' internally, so the overall character is unchanged. This also dropped the whole `image`/`makeCanvas` cover-fit-crop math (`createCardHolo()` no longer takes an `image` param at all) and let `mountHolo()` start immediately instead of behind an `img.onload` callback, since there's no longer anything to load first. Verified via Playwright: the canvas's parent is now `.tcg-card` and its bounding rect exactly matches the card's own rect, a drag starting from the header panel and a separate drag starting from the info panel (neither one touching the art window) both produce a real `matrix3d(...)` transform on `.tcg-card`, and `mix-blend-mode:screen` is actually applied; the full existing 5-rarity/no-scroll/back-button/onboarding-chaining regression suite still passes with zero console errors.
| 0.60.0 | 60 | pending | development | Two more rounds of feedback after director confirmed v0.59's whole-card tilt worked well. (1) "레전더리는 금색 에픽은 보라색으로 프리즘효과를 주고 히든은 레인보우로 효과를 주는게 더 좋을꺼같아" (gold for LEGENDARY, purple for EPIC, rainbow for HIDDEN) -- createCardHolo()'s spectrum gradient used one fixed rainbow palette for every rarity; new `holoSpectrum(rarity)` now returns a distinct 7-stop palette per rarity, passed through as a `data-spectrum` attribute on the canvas so mountHolo() itself stays generic. HIDDEN's palette is literally `foilTitleStyle('HIDDEN').colors` (the same rainbow its card title's foil-gradient text already uses) rather than a separately hand-picked array, keeping both effects in sync. HIDDEN was also added to the holo-effect-eligible tier alongside EPIC/LEGENDARY (v0.27 had excluded it since it already had its own dark theme, a rationale that no longer holds now that it gets a dedicated palette). (2) "저녁9시?에 통계로 알려줄때 앱을 열때 가장 많이 접근된 카드를 뙇하고 보여주면 더 좋을꺼같아 어떤어떤 사건 발견됨! 하면서 카드가 휘리릭 돌아가면서" (dramatically reveal the day's top card when opening the app, Pocket pack-opening style) -- new `revealScreen()` shows once per calendar day (`state.history.revealShownDate`) the first time refresh() finds a non-empty report, reusing detailPage()'s own `.tcg-card` markup and holo effect wholesale (same palette/frame/header/art/info/emblem) rather than building a second visual system, with only the surrounding chrome different: a circular flash burst, a "오늘의 사건 발견!" title, and a one-shot spin-in entrance animation on the card itself instead of appearing flat, followed by a single continue button. Wired into the back button the same way every other full-screen overlay is. Two real bugs fixed while building this: `fitTcgCard()`/`mountHolo()` measured card/canvas size via `getBoundingClientRect()`, which reflects the element's current CSS transform -- reading that mid-reveal-animation (while the card is still scaled down as part of its entrance) or mid-drag-tilt could bake a wrong size into the canvas backing store or the auto-fit width; switched both to `offsetWidth`/`offsetHeight` (unaffected by transform), a general robustness fix beyond just this feature. Also added a `?reveal=0` test-only escape hatch (same pattern as the existing `?onboarding=0`) since the reveal otherwise arms on every fresh preview load with no localStorage, which was swallowing every existing Playwright test aimed at the screens underneath it. Verified via Playwright: each rarity's `data-spectrum` differs and screenshots show the distinct gold/purple/rainbow tint, the reveal arms on a fresh load and the card settles at full size with `transform:none` once its entrance animation finishes, dismissing it persists `revealShownDate` and a same-day `refresh()` does not re-arm it; the full existing 5-rarity/no-scroll/whole-card-drag/onboarding-chaining/back-button regression suite (run with `?reveal=0`) still passes with zero console errors.
| 0.61.0 | 61 | pending | development | Director feedback: "근데 아직도 레전더리랑 에픽이 잘나오네;; 역으로 노멀이랑 레어는 잘 안나오는거아니야?" (LEGENDARY/EPIC still show up constantly; NORMAL/RARE barely show up at all, right?) -- confirmed as a real structural bug in `IncidentDetector.resolve()`, not just a threshold-tuning gap left over from v0.51 (which only ever raised REENTRY's own thresholds). `resolve()` put HIDDEN/LEGENDARY into a completely uncapped "special" bucket (every single LEGENDARY/HIDDEN incident detected that day made the report, with no limit) while pooling NORMAL/RARE/EPIC together into one shared "ordinary" bucket capped at 3 by raw score -- and EPIC's baseScore (60) so reliably beats RARE's (30) and NORMAL's (10) in that shared pool that a single EPIC incident could crowd every NORMAL and RARE incident out of the day's report entirely. Replaced the two-bucket split with five independent per-rarity caps (HIDDEN 1, LEGENDARY 1, EPIC 2, RARE 2, NORMAL 3 -- smaller cap for a rarer tier), so no tier can ever crowd another out of the report; a tier's presence still depends on it actually being detected that day (these are ceilings, not guarantees), but competition is now scoped within each tier instead of across all of them. Verified with a new JVM-only standalone script (`core_test_harness_verify/VerifyRarityCaps.kt`, using QUICK_EXIT's own deterministic count thresholds across 8 synthetic packages to reliably produce 1 LEGENDARY + 2 EPIC + 2 RARE + 3 NORMAL candidates in one day) and a `git stash`-based before/after comparison on the exact same scenario: the old code kept LEGENDARY=2 (uncapped, a coincidental DAWN_SURVIVOR tie included), EPIC=2, RARE=1, and NORMAL=0 (entirely crowded out); the fixed code keeps LEGENDARY=1, EPIC=2, RARE=2, NORMAL=3 (every tier represented, none unbounded). The existing `DetectorSmoke`/`DetectorRegression` JVM harness tests still pass unchanged, confirming no other detection logic was touched. This is a pure Kotlin change in the dependency-free `core` module, so unlike the NativeBridge.kt changes in v0.55-v0.58 (Android-framework classes this session's tooling can't execute), it was fully exercised and verified end-to-end via the JVM harness, not just checked for compilation.
| 0.62.0 | 62 | pending | development | Director feedback: "그 앱이름 보여주는거 말이야 ... 그건 알아보기 힘든 이름이 아니라 앱자체이름을 가지고 올 수는 없는거야?" (the app-name display thing -- can't we get the app's own real name instead of that hard-to-read one?). v0.51 already added `NativeBridge.appLabel()` (real app label via PackageManager) with `compactPkg()` falling back to the old package-name-derived guess on failure -- this feedback meant it was still failing on the director's real device, so the root cause had to be somewhere the code itself couldn't show: Android 11+ (API 30+) package visibility restrictions. A targetSdk-30-or-above app (this one targets 36) querying `PackageManager.getApplicationInfo()` for another installed app now throws `NameNotFoundException` unless that other app is declared reachable via a `<queries>` element in the manifest (or a few narrow exceptions) -- with no such declaration, `appLabel()`'s existing `runCatching{...}.getOrDefault("")` silently absorbed the failure for virtually every real installed app, and `compactPkg()` silently fell back to the old ugly display every time; the v0.51 code itself was correct, just missing this one manifest declaration to actually work on real devices. Rather than `QUERY_ALL_PACKAGES` (a sensitive permission requiring separate Play Console justification), added the manifest-only `<queries><intent><action .../MAIN><category .../LAUNCHER></intent></queries>` block Google specifically recommends for this exact case -- it opens visibility only to apps with a home-screen launcher icon (i.e., exactly the apps a user would actually recognize by name), nothing broader. Caught and fixed a second near-miss of the same class of bug as v0.47's manifest issue while drafting this change's own comment: it initially used literal `--` as punctuation three times, which would have prematurely closed the XML comment exactly like before -- caught this time by running the established `python3 -c "import xml.etree.ElementTree as ET; ET.parse(...)"` plus a regex scan for literal `--` inside comment bodies immediately after writing the comment, before ever committing it. This is a manifest/permission-declaration-only change with no Kotlin logic to exercise, so it's verified by XML-parse success and a green CI build; whether app names actually resolve correctly now needs a real-device check.
| 0.63.0 | 63 | pending | development | Director asked for a general proactive review: "또 따로 필요한거나 이런것도 있으면 더 좋겠다 라던가 수정할 것이라던가 등등 찾아보렴". Re-read `docs/OPEN_ISSUES_AND_NEXT.md` and cross-checked the codebase for similar bug classes; found and fixed two things. (1) A real, previously-undiscovered bug in the incident detail page's stat rows: `metricLabel()` was missing translation cases for 5 of the 13 actual metric keys `IncidentDetector.kt` emits (cross-checked every key used in its `mapOf(...)` calls against the switch) -- `afterUnlockMs` (FIRST_CONTACT), `avgStayMs` (APP_WANDERING), `totalUsageMs` (HIDDEN_NIGHT_ACTIVITY), and `visits` (RETURN_TO_START -- actually seen firsthand in a v0.53 screenshot this session and shelved as out-of-scope at the time) had no case at all and fell through to the raw English key; `unlockSessions` (HIDDEN_NIGHT_ACTIVITY) had a case under the wrong name (`unlocks`), making it dead code with the same effect. Same class of gap as v0.38's `windowMs` fix. Added all 5. (2) Added the backup schema version field that `docs/OPEN_ISSUES_AND_NEXT.md` had flagged as outstanding since v0.47: both `save()` (localStorage/native backup) and `exportBackupData()` (settings -> export data) now include `schemaVersion:1` in their payload; `restore()` still only reads `settings`/`history` so this doesn't change how existing backups load, it just gives a future `restore()` something to branch on if the storage shape ever needs to change. Flagged (not code-fixable) the still-outstanding privacy-policy placeholder text (`[add a contact email]`/`[add a date before launch]`) that needs the director's actual contact email and launch date before any store submission. Verified via Playwright: all 13 real metric keys now resolve to a translated label (zero raw-key fallthroughs), a RETURN_TO_START card's detail page screenshot confirms "visits" now reads "방문 횟수" instead of the raw key, the backup schema version round-trips correctly through save()/restore(), and the full existing regression suite (back button, onboarding-to-usage-access chaining, 5-rarity detail pages) still passes with zero console errors. `docs/OPEN_ISSUES_AND_NEXT.md` updated to mark both items resolved.
| 0.64.0 | 64 | pending | development | Follow-up to v0.63's flagged privacy-policy placeholders: director provided the missing values -- "이메일은 헬로우 투데이와 같고 출시 전 날짜는 오늘날짜라도 상관없으면 그렇게 해줘" (use the same email as the Hello, Today app, and today's date is fine for the launch/last-updated date). Confirmed the actual contact email used by "Hello, Today" (another app in this monorepo, `hellotoday-app/` on the `HelloToday` branch) by reading its own privacy/특정商取引法 disclosure text directly: `db5704@gmail.com`. Filled both placeholders into all three language variants (Korean/Japanese/English) of the privacy policy sheet -- contact email and a natural-language "last updated" rendering of today's date per locale. The sheet's own code comment now documents why these specific values were used (director's instruction plus the Hello, Today cross-reference) so a future real legal review has that context; the sheet still isn't reviewed legal copy, and that caveat is kept in the text. Verified via Playwright that the sheet renders with zero remaining bracket placeholders and the correct email/date; full existing regression suite still passes with zero console errors. `docs/OPEN_ISSUES_AND_NEXT.md` updated accordingly.
| 0.65.0 | 65 | pending | development | Director's explicit monetization go-ahead after a 4-option discussion: option 1 (one-time "remove ads" IAP + a small cosmetic bonus) plus option 2 (banner ads), with the ad surface extended beyond Records/Archive to also cover the back-button exit-confirmation screen; option 3 (individual cosmetic IAP) explicitly deferred until the app "grows bigger." This is the app's first-ever network permission -- v0.64 was fully offline -- since both AdMob and Google Play Billing require it. Ad network/billing library choices (Google AdMob, Google Play Billing) are the only sensible defaults on Android and weren't separately confirmed by name, consistent with this session's pattern of proceeding on clear explicit direction. Built with Google's own official TEST App ID / ad-unit IDs throughout (`ca-app-pub-3940256099942544~3347511713` etc.) -- these render real test-mode ad creative with zero real-money/policy risk and MUST be swapped for the director's own real AdMob account IDs (create the app + ad units at admob.google.com) before this generates real revenue; likewise the `remove_ads` one-time product must be created in Play Console before a real purchase can succeed. New `AdManager.kt` (MobileAds init, banner AdView, interstitial load/show, respects the ads-removed flag) and `BillingManager.kt` (BillingClient wrapper: query product, launch purchase flow, acknowledge, persist `ads_removed` in the same `app_prefs` SharedPreferences file `setLanguage()` already uses, restore purchases on start). `MainActivity.kt`'s bare `setContentView(webView)` restructured into a vertical `LinearLayout` (WebView weight=1 + a banner `FrameLayout`, hidden by default) since a native `AdView` can't live inside the WebView's own HTML; both managers are created and started in `onCreate()`. `NativeBridge.kt` gained `setBannerVisible()`, `isAdsRemoved()`, `purchaseRemoveAds()`, `restorePurchases()`, and `exitApp()` now shows the loaded interstitial (if any, and if ads aren't removed) before finishing instead of finishing immediately. index.html's `render()` gained a `syncBannerVisibility()` helper called on every render (not just tab switches, since the reveal/detail/onboarding overlays fully replace `#app`'s content without going through `setTab()`) so the banner shows only while the current tab is Records/Archive and nothing is covering it; settings gained "광고 제거"/"Remove ads" (purchase or, once bought, a static "purchased" row) and "구매 복원"/"Restore purchase" rows, reading `N.isAdsRemoved()` fresh every render so the row flips automatically once `onNativeResume` re-renders after returning from the native purchase sheet, with no extra native-to-JS push needed. Added a new "Ads & in-app purchase" section to the privacy policy (all 3 languages) disclosing that AdMob/Play Billing are separate Google services requiring network access and able to handle device info like an advertising ID, since the prior "everything stays on this device" claim (just finalized in v0.64) would otherwise have become materially inaccurate -- carefully scoped to say this app's own usage-analysis data still never leaves the device. AndroidManifest.xml gained `INTERNET`/`ACCESS_NETWORK_STATE` permissions and the AdMob `APPLICATION_ID` meta-data (caught and fixed two literal `--` sequences in its own new comments before pushing, same XML-comment bug class as v0.62, via the same `ET.parse()` + regex-scan habit); `app/build.gradle.kts` gained the `play-services-ads`/`billing-ktx` dependencies. Verified: the JS side (banner visibility across every tab/overlay transition, settings row rendering and state-flip, purchase/restore call wiring, the new privacy-policy section, the untouched exit-confirm sheet) end-to-end via Playwright with a stubbed native bridge, zero console errors; CSS rule count re-confirmed unchanged (213). Kotlin (`AdManager`/`BillingManager`/`MainActivity`/`NativeBridge`) can't be compiled locally in this sandbox (no Android SDK/Gradle dependency access, a documented pre-existing constraint -- confirmed again this session when a local `gradle :app:assembleDebug` failed to resolve the Android Gradle Plugin itself, unrelated to this change) -- verified via brace/paren balance checks and manual review, with final compile verification going through `.github/workflows/build-opened-again-gradle.yml` CI (the only place a real `assembleDebug` has ever run for this app). Two follow-up fixes landed under this same version number before CI first went green: (1) the initial push failed CI with `BillingManager.kt:86: Unresolved reference 'productDetailsList'` -- `queryProductDetailsAsync()`'s callback was written against `QueryProductDetailsResult` (Play Billing 8+'s real shape) but the dependency was mistakenly declared as `billing-ktx:7.1.1` (whose callback instead passes a plain `List<ProductDetails>`). (2) The director asked whether this app was built against Google Play's current requirements; a web search confirmed it was not fully -- Google Play now REQUIRES Billing Library 8+ for any new app/update (deadline Aug 31 2026, extendable to Nov 1 2026), so 7.1.1 was already non-compliant independent of the CI error, and 23.6.0 for `play-services-ads` was two major releases behind the current 25.0.0. Fixed by bumping to `billing-ktx:8.3.0` and `play-services-ads:25.0.0` (keeping the code's original `QueryProductDetailsResult`-shaped call, which was correct all along -- the dependency version was the actual bug, not the code) rather than downgrading the code to match the older API. Also confirmed Google named a separate "GMA Next-Gen SDK" the preferred choice for brand-new ad integrations starting July 2026, but legacy `play-services-ads` isn't deprecated until June 2027 / sunset until June 2028 -- deliberately stayed on the mature latest-legacy line for this first ads integration rather than an SDK announced only ~2 months earlier, flagged in `docs/OPEN_ISSUES_AND_NEXT.md` as a migration to revisit before the 2027 deadline. `targetSdk 36` was already compliant with the Aug 31 2026 minimum, no change needed there.
| 0.66.0 | 66 | pending | development | Director feedback: "그 사건 카드들 설명이 너무 불친절한거같아 무슨앱을 들어갔는지 어떤일이일어나서 이걸얻었는지 자세히 알려줬음 좋겠음" (incident card descriptions feel unfriendly -- want to know exactly which app and what happened). The old `detail(i)` only ever showed a bare stat ("Detected 5 short opens") with no app name, even though 7 of the 14 incident types already carried `i.primaryPackage` from `IncidentDetector.kt` -- it was just never used in the description text (only shown separately as a small pill on the featured card). Rewrote `detail(i)` so every type reads as a real sentence naming the actual app(s): the 7 single-app types (QUICK_EXIT, REENTRY, ESCAPE_FAILED, REGULAR, HUNDRED_VISITS, FIRST_CONTACT, RETURN_TO_START) now use `compactPkg(i.primaryPackage)` directly (e.g. "Opened KakaoTalk briefly 7 times and closed it right away"). For the 6 multi-app "wandering"-style types (PATROL, APP_WANDERING, NIGHT_PATROL, DIGITAL_LOST, DAWN_SURVIVOR, HIDDEN_NIGHT_ACTIVITY), which always had `primaryPackage=null` since they're inherently about crossing many apps, added a new `DetectedIncident.topPackage` field (core module) -- the single most-visited app within that incident's own session/window, computed by a new `mostVisitedPackage()` helper and surfaced purely for display text; critically, `resolve()`'s day-level dedup/rarity-cap grouping still keys only on `primaryPackage` (unchanged), so this is a pure additive change with zero effect on how many cards surface per day or which rarities win. HIDDEN_LOOP -- literally "two apps alternating," yet the only type that never named either app ("두 앱 사이", between two apps, no names) -- got a new `secondaryPackage` field alongside `primaryPackage` now naming both apps (verified the `primaryPackage` change here is still safe: HIDDEN-rarity cards are already capped at 1/day since v0.61, so regrouping by real app names instead of a shared `null` key cannot change the final card count). Both new fields default to `null` (backward compatible with existing saved data) and flow through `NativeBridge.incidentToJson()`'s JSON. Every type keeps a same-meaning fallback sentence for the case a name genuinely isn't available (e.g. an empty visit list), so this is additive, not a breaking rewrite. Verified: (a) core module via the existing JVM harness (`DetectorSmoke`/`DetectorRegression`, unaffected and still passing) plus a new ad-hoc verification script confirming PATROL correctly picks the actual most-visited app as `topPackage` and HIDDEN_LOOP names both real apps; (b) JS side via Playwright across all 14 types (plus the 2 no-app-data fallback cases) confirming non-empty, app-named output, plus screenshots of the home tab's featured card (multi-line `.detail`, wraps cleanly) and the incident detail page (single-line `.detail-sub`, fits without breaking the existing one-screen-fit layout); the compact case-row list still single-line-ellipsis-truncates on long sentences (a pre-existing CSS constraint, not new), but the app name now sits early enough in each sentence to survive truncation. CSS rule count re-confirmed unchanged (213); full existing regression suite (back button, onboarding, reveal, 3-rarity cards, whole-card drag, backup schema, metric labels, ads/banner visibility) still passes with zero console errors.
