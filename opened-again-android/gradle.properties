org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# v0.38: flipped true -- app/build.gradle.kts added androidx.core:core
# (ShareCardRenderer's FileProvider, see AndroidManifest.xml) as this
# project's first AndroidX dependency. AGP's :app:checkDebugAarMetadata
# task hard-fails the build whenever an AndroidX artifact is on the
# classpath while this flag is still false (confirmed via CI run #39).
android.useAndroidX=true
kotlin.code.style=official
